# URL base de la API del backend (incluye /api).
# En esta maquina el backend corre en el puerto 18081 (ver backend/src/main/resources/application.properties).
VITE_API_BASE_URL=http://localhost:18081/api
